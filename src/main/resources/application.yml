spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/gastei
    username: postgres
    password: admin
    driverClassName: org.postgresql.Driver
server:
  servlet:
    contextPath: /gastei